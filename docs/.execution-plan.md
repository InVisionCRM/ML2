## Execution Plan (requires approval)

**Goal**
Implement PLAN.md: rename MORBIUS→MORBIUS, add read helpers, add house-to-Mega ticket, regenerate ABI.

**Steps**
1) Refactor contract naming
   - In `contracts/contracts/SuperStakeLottery6of55V2.sol`: rename MORBIUS token refs (vars, params, events, state) to MORBIUS; update comments/revert strings.
   - Rename banks/fields: currentRoundTotalMORBIUS→currentRoundTotalMORBIUS, pendingRoundMORBIUS→pendingRoundMORBIUS, megaMORBIUSBank→megaMORBIUSBank, etc.
   - Update events args names (`MORBIUSSpent`→`MORBIUSSpent`, etc.).

2) Add read helpers (view)
   - `getCurrentRoundTotals()`: roundId, totalMORBIUS, totalTickets, uniquePlayers, rolloverReserve, megaMORBIUSBank, currentRoundState.
   - `getPendingForRound(uint256)`: pendingMORBIUS, pendingTickets.
   - `getRolloverState()`: rolloverReserve, megaMORBIUSBank.
   - `getBracketConfig()`: BRACKET_PERCENTAGES + top-level distribution (winners/burn/mega).
   - `getUnclaimedForRound(uint256)`: per-bracket pool/winners/paid/unclaimed + totals.
   - `getTotalTicketsEver()`, `getTotalMORBIUSEverCollected()`, `getTotalMORBIUSEverClaimed()`, `getTotalMORBIUSClaimableAll()` (maintain counters on finalize/claim).
   - `getPlayerLifetime(address)`: ticketsBought, totalSpent, totalClaimed, totalClaimable.
   - `getPlayerRoundHistory(address,uint256,uint256)`: paginated player records.
   - `getRoundHistoryTotals(uint256)`: totals for a round (collected, winners pool, burn, mega contrib, unclaimed/rollover).
   - Expose finalized totals for UI fallback when current round is zero.

3) House ticket → MegaMORBIUS
   - Add flag/config; on round start, mint one internal ticket; if it wins, payout goes to megaMORBIUSBank.
   - Funding: reserved balance (no user funds).
   - Use same RNG/path as user tickets; mark for mega diversion.

4) ABI + regen
   - Regenerate `abi/lottery6of55-v2.json`; update TS exports/imports to MORBIUS naming.

5) Post-checks
   - Ensure storage variable renames align with migration/redeploy plan.
   - Run lints/compile.

**Notes**
 - Ticket price stays 1e18 MORBIUS; distribution 60/20/20 unchanged.
 - Avoid iterating large storage; keep running counters updated on writes.

